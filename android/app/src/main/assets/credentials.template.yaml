# Store sensitive credentials here
# Copy this file to credentials.yaml and fill in your own values
# credentials.yaml should be excluded from version control

# OAuth client credentials
clientUrl: "your-strato-instance.blockapps.net"
clientId: "your-client-id"
clientSecret: "your-client-secret"

# User common name for asset lookup
userCommonName: "your-common-name"
